git clone https://github.com/mrbilalshafiq/frontend-and-backend-python
cd frontend-and-backend-python
docker-compose up
docker-compose up -d
docker-compose logs
docker-compose logs -f
git checkout red-background
docker-compose build
docker-compose up -d
curl localhost:5000
git checkout main
docker-compose build
docker-compose up -d
curl localhost:5000
docker-compose down --rmi all
